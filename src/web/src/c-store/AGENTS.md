<!-- Generated by scripts/generate_ai_collab_docs.py. Edit metadata in that script. -->

# Cook Web Domain: c-store

This file adds local guidance for `src/web/src/c-store`. Read the repository
root and `src/web/AGENTS.md` first, then use this file for domain-specific
frontend ownership.

## Scope

- This domain owns legacy stores for course, system, environment, and UI
  layout state used by the `c` experience.

- Representative files in this subtree:
  `src/web/src/c-store/useCourseStore.ts`,
  `src/web/src/c-store/useSystemStore.ts`,
  `src/web/src/c-store/useUiLayoutStore.ts`, `src/web/src/c-store/envStore.ts`

- Keep durable frontend rules here only when they are more specific than the
  Cook Web-wide guidance in the parent directory.

- Use focused frontend verification around `src/web/src/c-store/` before
  widening to global type and lint checks.

## Do

- Start from the local entry files and preserve how `c-store` currently
  separates route code, shared logic, state, and utilities.

- Keep legacy store shape stable because `c` pages and services often depend
  on selector names implicitly.

- Preserve the relationship between layout constants, system flags, and route
  behavior in the `c` experience.

- Treat store updates as integration changes that may need coordinated
  service, constant, and page updates.

## Avoid

- Do not rename store fields or actions without updating all selectors,
  derived helpers, and consumers together.

- Do not move long-lived legacy state into page-local state just because a
  single page currently owns the change.

- Do not duplicate layout-state calculations when shared constants and store
  helpers already exist.

- Avoid pushing domain-local rules up to shared docs unless another frontend
  subtree truly depends on the same constraint.

## Commands

- `rg -n "c-store" src/web/src` to locate call sites before editing this
  subtree.

- `cd src/web && npm run type-check` after TypeScript, store, hook, request,
  or route-contract changes.

- `cd src/web && npm run lint` before closing the task when shared frontend
  code or route files changed.

- `cd src/web && npm run test -- src/c-store/` for focused Jest coverage when
  this subtree already has or gains tests.

## Tests

- Add or update focused tests closest to `src/web/src/c-store/` for the happy
  path and the most important regression or business-code error path.

- Rerun type-checking and linting when exported props, store fields, hook
  contracts, or route behavior move.

- When a domain depends on shared request or normalization helpers, update
  those tests or add new ones in the same task.

- Document skipped UI, browser, or provider-dependent verification in the task
  summary when local automation cannot cover it.

## Related Skills

- src/web/skills/chat-layout-width-detection/SKILL.md

- src/web/skills/hook-contract-refactor-safety/SKILL.md

- `src/web/SKILL.md`

- `SKILL.md`

- Create a focused skill under `src/web/skills/<topic>/SKILL.md` when the same
  debugging or migration workflow repeats.
